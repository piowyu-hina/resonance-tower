# 爬塔掛機 設計筆記

> 這份文件以「目前程式實作」為準；舊方案只在有必要交代決策原因時保留。

## 當前開發範圍

- 現階段先以**無名**作為唯一完整打磨的角色，用他驗證戰鬥、成長、背包、商店、掉落與後續存檔等共用系統。
- 小初與豐子保留既有資料與解鎖流程，暫不擴充角色專屬內容；待共用系統穩定後再完成。
- **萊咪暫不上線**：已從 `CHAR_DEFS`、角色列表與解鎖流程移除；現有圖片素材保留在 `assets/`，不參與目前版本。

## 目前雛型狀態（已寫進 prototype/）

- 純 HTML/CSS/JS，無框架、無建置流程，`js/` 拆成 constants / state / goo / combat / ui / main 六個檔案。
- 玩法：單機模式一次只能帶 1 名角色（`SOLO_PARTY_LIMIT`），爬樓層。
- **小怪已陣列化（這次 session 的主要工程）**：`monster`（單一物件）改成 `monsters`（陣列），每波小怪同時出現 **2~3 隻**（隨機），各自獨立血量、獨立攻速倒數；角色攻擊隨機挑一隻存活小怪、小怪攻擊隨機挑一位隊員，都是雙向隨機。死亡判定集中在 `tick()` 尾端統一掃描（不管是被攻擊、技能、還是黏液小遊戲點死的都會抓到），死亡瞬間血條先歸零、卡片變暗閃一下，約 0.5 秒後才整張移除（不會卡在畫面上不消失）。
  - **範圍說明**：目前維持「兩波小怪、每波 2~3 隻、清完兩波才出王」的結構（`MOBS_PER_FLOOR` 沿用原本語意，只是每波人數變多）。design.md 原本規劃的「累計殺 10 隻觸發事件、殺滿 100 隻才出王」**這次沒有做**，因為那個需要事件系統配合、目前還沒設計，先維持簡單的波次結構，避免兩次工。
  - 小怪數值（血量/攻擊）整體砍半左右，抵銷「同時面對 2~3 隻」的傷害暴增，但只是粗調，之後還要細調。
  - 小怪 XP 貢獻share（`MOB_XP_SHARE`）從 0.25 調降到 0.1：因為現在一層樓要清 4~6 隻小怪（不是原本的 2 隻），沒調降的話小怪總經驗值會膨脹成原本的 2~3 倍。
- 角色技能已實裝；其中魔法護盾採「提升防禦」而非直接降低所受傷害：目前為隊伍防禦 +3、持續 6 秒，最後仍統一走「攻擊－防禦」傷害公式。
- **攻擊數值有隨機浮動**：`DAMAGE_VARIANCE = 0.15`，所有攻擊（角色/怪物、普攻/技能）都套用 ±15% 隨機浮動，不再是每次都打出完全一樣的數字。
- **怪物的技能現在有真正的冷卻**：黏液潑濺（傷害＋降攻速）不再是每次出手都套用，小怪 4 秒、王 5 秒的獨立冷卻，冷卻中改用不帶降攻速效果的普通攻擊補位，行為邏輯跟角色的技能/普攻機制對稱。
- **技能施放特效（`showSkillCastEffect`）改成看對象**：對敵方造成傷害的技能，特效會閃在敵方卡上；自身/隊友向的技能（自癒、buff、閃避、治療隊友）閃在真正受影響的那張卡上（施法者自己，或被治療的隊友），不是固定閃在施法者身上。
- 首領技能重新編號並接通：**skill（黏液潑濺）**維持不變；**skill2（召喚黏液）已實裝**，每 10 秒召喚一隻隨機史萊姆、場上最多兩隻召喚物；**skill3（黏液陣）**維持獨立的 `bossArena` 點擊小遊戲。三招都有各自的技能圖示與冷卻顯示。
- **撤退按鈕現在「準備階段」跟「王前確認」都會出現**（原本王前確認沒有撤退選項），玩家隨時可以見好就收。
- **準備階段標題下面的說明句拿掉了**：已經選好出戰角色時，原本會多顯示一行「已選好出戰角色，出發後整局鎖定」，玩家覺得多餘（卡片本身的選中樣式已經講清楚了）——只清掉這一種情境的文字，「請先選角色」跟「隊伍已鎖定」這兩種還有實際指引作用的訊息維持不變。
  - 「準備階段」標題字級太小被抓出來過一次：原本 10px 比標題列的「商店」按鈕（13px）還小，不像個標題。調到 14px，明確定案「至少要跟標題列按鈕字級一樣大以上」，之後其他標題文字也比照這個下限。
- **標題列圖示已補上**：商店按鈕現在也有圖示（`shop.png`，小攤販），跟背包按鈕並列。**UI 圖示統一移到 `assets/ui/`**（原本 `bag.png` 放在 `assets/item/` 裡，跟藥水/結晶這些遊戲道具混在一起，語意上不對——`assets/item/` 留給玩家背包裡真的會出現的物品，標題列這種操作介面用的圖示歸到 `assets/ui/`）。
- **素材資料夾整理過**：`assets/monsters/`、`assets/skills/` 底下，樓層 1 相關的怪物圖/技能圖都歸進 `floor1/` 子資料夾；`assets/item/` 底下，還沒接上程式邏輯、屬於「主動道具系統」預留的戒指類素材歸進 `active_items/` 子資料夾。分類原則：已經被程式碼引用的留在原資料夾，尚未接上邏輯的歸到依系統命名的子資料夾。

## 最新進度（角色解鎖地基＋玩家自行擴充的怪物/狀態/背包系統）

- **角色 id 與素材命名已統一**：目前上線資料為**無名**（`wuming`）、**小初**（`xiaochu`）與**豐子**（`fengzi`），不再使用職業或 `charaN` 編號。
- **角色稀有度**：純顯示用（不影響數值），無名=普通、小初=稀有、豐子=稀有；UI 以角色卡外框顏色與光暈呈現，不額外放文字標籤。**這條規則一度被破壞過又修回來**：中途曾經在卡片上加了瀏覽器原生 `title` 屬性顯示「普通／稀有」字樣（當初是想補色盲可讀性），結果原生 tooltip 樣式很醜、還會有殘留卡住不消失的問題，玩家明確表示不要，已整個移除。**以後不要再用原生 `title` 屬性放這種文字說明**，色盲可讀性之後如果真的要做，要用專案自己的樣式化 tooltip（`showTooltipContent`／`attachXxxTooltip` 那一套），不要用瀏覽器內建的。
- **角色解鎖地基已實裝**：小初＝累計擊殺 10 隻史萊姆；豐子＝累計成功使用 10 瓶藥水。兩條進度都已接上實際流程，且不受團滅影響。
- **怪物系統重構（玩家自行完成，非本次對話產出）**：`MONSTER_DEFS` 取代單一史萊姆，新增生氣/瞌睡/愛心/治療四種變種，技能用宣告式 `effects` 陣列描述（傷害/降速/睡眠/魅惑/治療），`combat.js` 統一用解釋器處理，不用為每種怪寫分支。
- **狀態效果系統**：新增 `STATUS_DEFS`（睡眠/魅惑/降攻速/加速/隱身/防禦提升），角色卡用通用狀態列渲染，不必為每種狀態各自寫 DOM。
- **首領技能擴充**：新增 skill2「召喚黏液」（`bossSummonTick`，場上最多同時 2 隻召喚物，開場 3 秒後才開始召喚，之後每 10 秒一次）——原本 design.md 規劃的「召喚小怪」已經真正落地。
- **豐子護盾技能改版**：從「傷害減免 %」改成「防禦力 +3 點」（`buffDefParty`），走跟一般傷害公式相同的 `calcDef()` 疊加，不再是獨立公式。
- **背包 UI 起步**：`inventory` 陣列（8 格）＋ `ITEM_DEFS`（藥水／魔物結晶），彈窗形式、支援拖曳交換格子，資料先塞測試用假資料（照原計畫「先做外殼＋假資料」）。
- **樓層上限**：新增 `MAX_IMPLEMENTED_FLOOR = 1`，打完目前唯一開放樓層的首領會直接自動撤退入袋，不會卡在無內容的樓層。

## 玩法方向（本次討論定案）

- **深耕樓層 1**，先不急著往上蓋更多樓層：增加史萊姆怪物種類、中途事件，樓層數暫時不擴張。
- **遺物：不另外做系統，直接等於「主動發動道具」**（見下方章節）。一開始討論時遺物被拿掉，後來決定「主動道具」這個第四招道具槽就是遺物概念的落地版本，不重複蓋兩套系統。
- **不做裝備／武器系統**：明確排除，跟遺物一樣，屬性成長只靠等級＋技能點，不額外加裝備欄位。
- **離線進度：先不做，保留態度**。「掛機」目前的定義是「戰鬥不用手動操作，開著看就好」（已經做到），不是「人不在、遊戲沒開也能累積進度」——後者需要伺服器算時間差，是完全不同量級的工程，之後有需要再重新評估。
- 樓層 1 的首領（史萊姆王）會被刻意調得非常硬，大約需要角色 10 級以上才有辦法穩定過關；等級曲線之後也會刻意拉長，變相強迫玩家掛機練等。
- **樓層 1 規模大幅拉長**：小怪改成每次出現 **2~3 隻同時**（**已實裝**，見上方「目前雛型狀態」）；**每擊殺 10 隻小怪觸發 1 次事件、累計擊殺 100 隻後才出現首領**這個更長的版本**還沒做**，這次先維持「兩波小怪、清完出王」的簡化結構，等事件系統設計出來再擴充成 kill-counter 版本。
  - **尚未解決**（等真的要做 100 隻版本時要面對）：本局跑到很後面（例如第 95 隻）才團滅，前面累積的戰利品會整批蒸發，只差臨門一腳——這是刻意要的高風險感，還是要加一個中途保底機制，還沒定案。
- **王的 skill2「召喚黏液」已實裝，直接複用小怪陣列與怪物定義**：每 10 秒隨機召喚 1 隻樓層 1 史萊姆，場上最多同時存在 2 隻召喚物；召喚物死亡會正常提供金幣與經驗，但不計入永久史萊姆擊殺解鎖進度。王死亡即判定王戰結束，剩餘召喚物一併清除。
  - **史萊姆變種素材已經歸位、且確認過用途**：`slime.png`（綠，一般樓層小怪）＋ `slime_angry`／`slime_sleepy`／`slime_love`／`slime_heal` 四個變種（**這些變種本來就是樓層 1 一般小怪池，不是「召喚專用池」**——之前一度誤把它們歸類成 summon_pool 資料夾，已經改名成 `assets/monsters/floor1/`，跟一般小怪放一起；召喚小怪只是之後會重用同一批外觀，不是它們的主要用途）。
  - **史萊姆變種與技能已實裝，採資料驅動架構**：怪物定義只宣告目標與效果，戰鬥流程由共用的目標選擇器／效果執行器處理，不依怪物名稱寫死判斷。一般小怪池會隨機出現普通、生氣、瞌睡、愛心、治療五種史萊姆。
    - **生氣史萊姆**：高傷害攻擊，造成普通攻擊 1.8 倍傷害。
    - **瞌睡史萊姆**：使玩家睡著；玩家攻速條照常跑完一次，該次行動用來醒來、不會攻擊。
    - **愛心史萊姆**：魅惑玩家，讓玩家下一次行動改為攻擊隨機存活友軍；單人模式沒有其他友軍時會攻擊自己。
    - **治療史萊姆**：治療怪物方生命比例最低的受傷同伴，恢復其 25% 最大生命；沒有受傷目標時改用普通攻擊。
- **角色卡狀態列已實裝**：睡眠、魅惑會持續顯示到下一次行動結算；降攻速、加速、隱身、護盾會顯示狀態名稱與剩餘秒數。狀態列由共用定義資料產生，不為每種狀態各自建立 DOM。
- 狀態圖示使用 `assets/effect_icon/` 素材；若某個狀態沒有對應圖片或圖片載入失敗，不再用 emoji 代替，而是保留一個紅色虛線空框，hover 說明會標示「缺少狀態圖示素材」，提醒之後補圖。狀態說明固定顯示在圖示上方；狀態節點不再每個 tick 重建，修正 hover tooltip 卡住不消失的問題。
  - 召喚小怪、雙王這類「王戰進階機制」保留給架構本身（不設限），但史萊姆王現在先用「召喚黏液」這一種就好，雙王留給之後更後面的王。
- 中途不會有免費回血，只能靠藥水或法師的治療技能。
  - **已知的設計張力（尚未解決）**：目前是單機限定只能帶 1 人，如果那把沒帶法師，整趟遠征就完全沒有技能回血、只能靠藥水。這在未來開放多人連線、可以同時帶三人時會很合理，但現在等於「治療是賭有沒有帶到法師」而不是玩家的策略選擇。先接受這個狀態，之後看要不要調整。

## 團滅／撤退規則（v3：退回「只損失本局未入袋部分」，**已實裝**，取代 v2 的整倉庫歸零）

- **等級／經驗／角色解鎖：永久保留，團滅不再歸零**。理由：史萊姆王需要 10 級以上、等級曲線又刻意拉長，「逼玩家掛機練等」這個目的已經靠曲線本身達成，不需要再疊加一層「團滅全部歸零」的懲罰，避免玩家覺得懲罰太重直接放棄。
- **團滅只損失本局未入袋的部分，已經安全存放的東西不受影響**：`runGold`（本局金幣）跟本局撿到還沒入袋的道具（`runInventoryGains` 追蹤的部分）團滅時會蒸發，但 `bankedGold`（永久金幣）跟先前已經安全帶回、存進倉庫的道具**不受影響**。
  - 這條規則中途改過兩次：最早的規劃就是這版「只蒸發本局未入袋」；某次 session 一度改成「整個倉庫（`bankedGold` 含已有道具）都歸零」，理由是覺得原案懲罰不夠重；但這個「掛機」遊戲的核心賣點是低壓力、放著就好，史萊姆王又刻意調得很硬、會常打常死，兩者疊加等於「離線時的一場團滅可能洗掉玩家所有累積」，太重了，所以**退回最早這版**。之後如果史萊姆王碎片/結晶這類「打王累積」的道具上線，也是走同一套規則——本局現拿到的碎片沒帶回家就會不見，先前已經安全存放的不影響，這樣「打王攢碎片」的進度感才有意義。
  - **跟 `endRun()` 的實作已經同步**。
- 撤退成功才會把本局金幣（`runGold`）跟本局撿到的道具正式併入永久倉庫。
- 樓層永遠重置回 1，不管撤退還是團滅。

## 存檔系統

- 存檔要能在家用電腦跟公司電腦之間同步，走 **Google 雲端硬碟同步的檔案**，不是純瀏覽器 `localStorage`（否則兩台電腦各自一份、對不上）。
- 格式：**不存純 JSON 明文**，但已經確認前端架構下不存在「真正防破解」這回事（解碼邏輯一定要跟著 JS 送到瀏覽器，行家看得到）。採用務實折衷：**Base64 編碼＋checksum 校驗碼**——存檔內容看起來不是明文、肉眼讀不出來，讀檔時重新驗證 checksum，兜不上就視為毀損拒絕載入，可以擋住手滑亂改數值，但擋不住真的懂行、刻意要破解的人。
- 尚未決定：實際存檔檔名/路徑、兩台電腦幾乎同時遊玩時的衝突處理方式（例如都各自產生新進度、Google 雲端硬碟同步時互相覆蓋的狀況）。
- **長期野心（先不做，記下來）**：之後想把存檔「做大做強」——改成真的串伺服器、由伺服器記錄玩家資料，而不是本機檔案＋雲端硬碟同步這種折衷方案。跟上面的 Base64＋checksum／Google 雲端硬碟方案是兩個不同量級的東西：伺服器版本要處理帳號系統、資料庫、多端同步的正確性等，屬於「之後有餘力再重新設計」的方向，目前仍先照現有方案走，不要因為這個野心卡住手邊的開發。

## 經濟系統

- 金幣延續現有的 `runGold`（本局未入袋，團滅會歸零）／`bankedGold`（永久保留）兩層結構。
- **商店分內外兩種**：
  - 地城內商店：花 `runGold`，有風險（團滅就跟著蒸發）。
  - 城外（安全區）商店：花 `bankedGold`，永久生效。
- 背包、物品堆疊、藥水裝備／使用、掉落追蹤與商店買賣已有可運作的資料結構；主動遺物與合成尚未實作。

## 經驗書／技能點強化（取代原本「等級換技能點」的方案，**已實裝**）

- **原本規劃「扣等級換技能點」已推翻**：討論中發現用「目前等級」當可轉換的資源，在掛機遊戲裡會被無限刷——前期升等很便宜，玩家可以「扣一級→放著刷回來→再扣一級」無限循環，等於這個「硬取捨」形同虛設。
- **改用怪物掉落的道具當技能點的唯一來源**：跟魔物結晶完全同一套機制（怪物擊殺機率掉落、進背包、可疊），不是靠重刷便宜的低等級，天然沒有上面那個漏洞。
- **書分成「能力書」跟「技能書」兩種、各自獨立的貨幣**（不是單一種經驗書）：討論時覺得直接分級（初階/中階/高階…像主動道具那樣）沒有意義——現在只有樓層 1，沒有更高樓層/更強的怪可以掉高階書，分級只是換皮。改成依「性質」分兩種更合理：**能力書**管攻擊力／防禦力／攻速這三條「讓角色整體更耐打」的線；**技能書**管 3 個自動技能「讓招式更猛」的線。玩家可以自己選要往哪個方向刷，不是打開角色詳情看到 7 條線齊頭並進。
  - 兩種書**掉落機率各自 15%**（`SLIME_STAT_BOOK_DROP_CHANCE` / `SLIME_SKILL_BOOK_DROP_CHANCE`，各自獨立判定，不是原本單一 15% 拆一半——拆一半會讓點滿一條線的時間直接翻倍，破壞原本抓的節奏）。
  - 掉落規則跟結晶一樣：一般、非召喚的史萊姆才會掉；召喚物不掉，避免刷首領無限出書。
- **每個角色有 7 條獨立的強化線**：攻擊力／防禦力／攻速（吃能力書）三條基礎屬性，加上該角色 3 個自動技能＋**專屬操作**各自一條（都吃技能書），全部 0~100 級（`STAT_LINE_MAX`）。
  - **每條線每級固定消耗 1 本對應的書**，點滿 100 級＝該數值變基礎值的 **2 倍**（`lineScale()`：1 + 等級/100）。
  - 攻擊力／防禦力線：直接乘上角色的 `atk`／`def`（跟 `partyBuff`／`partyDefense` 這類暫時性 buff 疊加時是分開的乘數，不會互相污染）。
  - 攻速線：點滿讓該角色 `atkInterval` 減半（等於出手頻率變 2 倍），跟遊戲裡「迅捷藥水」的加速語感一致。
  - 技能線：對應那個技能自己的效果數值（傷害倍率／治療%／buff 幅度）乘上同一個 1~2 倍的比例；其中「加速」這類技能本身的 `mult` 是「越小越強」，直接套用會算反方向，改成放大「加速幅度」（`1-mult`）再算回新的 `mult`，並設 0.95 上限避免點滿後間隔被砍到接近 0。
  - **專屬操作線**（新增）：無名的「臨機應變」是隨機觸發自己 3 個技能之一，本身沒有可以放大的傷害/治療數值（抽中的技能已經各自吃自己的技能線加成），所以專屬操作線改成點滿讓**該操作自己的冷卻減半**（跟攻速線的處理邏輯一樣），變相讓玩家更常用到這個手動操作。之後如果有角色的專屬操作本身就有傷害/治療數值，可以比照技能線再加一層倍率疊加。
- **角色養成 UI 已改為單層面板（取代舊的彈窗疊彈窗版本）**：左側角色立繪與出戰按鈕；右側標題列直接顯示能力書／技能書庫存，下面是基礎能力卡＋技能卡（`growthCard`，可點選），底部一個養成檢視區（`growthInspector`）顯示目前選中那條線的「目前效果 → 下一級效果」、所需書本與升級按鈕。
  - 能力、自動技能與專屬操作共用一套互動：先點卡片選線，底部養成檢視區跟著換內容。
  - 按一下升 1 級；按住按鈕每 90ms 連續升級（`attachHoldRepeat()`），滿級或書本用完會自動停止，不會浪費最後一本書。
    - **修過一次 bug：長按時畫面要跟著逐格跳，不能放開才一次跳到底**。原本 tick 只呼叫 `useExpBookOnLine()` 改資料、完全沒更新畫面，DOM 只在放開按鈕那一刻整個面板重繪一次，玩家看起來像是「按下去卡住、放開才突然跳到 20 級」。改成每個 tick 都呼叫 `updateGrowthPanelLive()`，直接 patch 卡片角標／養成檢視區的數字，**不重繪整個面板**——如果每 90ms 都整個 `renderCharacterDetail()`，等於把 `#growthUpgradeBtn` 這個按鈕本身連著換掉，長按用的 `pointerup` 監聽器會跟著失聯，長按會卡住停不下來。放開按鈕那一刻才整個面板重繪一次，確保收尾資料一致。
  - **「按住按鈕可連續升級」提示字拿掉了**：玩家覺得沒用，加上長按現在會逐格跳、視覺上自己就會教會玩家，不需要文字說明。
  - 效果數字改成顯示到小數點（不再四捨五入成整數），避免低等級時「目前→下一級」兩邊數字一樣、讓玩家以為沒生效。
  - 專屬操作卡（臨機應變）固定套外框跟其他 3 個技能卡區分；技能區塊標題精簡為「技能」。**外框顏色定案：專屬操作＝金色、目前選中＝藍色**（`#5aa9e6`）。中途試過反過來（選中金色、專屬操作青色），玩家覺得不對——金色在這個介面裡本來就有「特殊/專屬」的既有語感（書本圖示、獎勵色都是金），選中狀態改用新顏色比較合理，不要去搶本來就該是金色的東西。
  - 基礎能力三張卡（攻擊力／防禦力／攻速）從文字符號圖示（⚔◈⌛）換成真的圖：`assets/skills/stats_atk.png`（劍）／`stats_def.png`（盾）／`stats_speed.png`（沙漏）。
  - 面板整體字級調大一輪（原本 8~11px 偏小偏糊，多數文字調到 11~13px）。
  - `#characterDetailModal` 補上 `user-select: none` 跟 `-webkit-user-drag: none`：原本在面板上拖曳滑鼠會出現文字反藍選取／圖片拖曳殘影，跟遊戲操作手感不搭，關掉。
  - **已修過一個 bug**：角色詳情最上面顯示的攻擊／防禦／行動間隔，原本讀的是角色原始 `c.atk`/`c.def`/`def.atkInterval`，沒有套用屬性強化的加成，導致玩家點了線、數字卻沒變。現在改成顯示 `c.atk * lineScale(c,'atk')` 等實際生效的數值（不含戰鬥中的暫時性 buff，只反映永久成長），點線之後畫面會立刻反映出來。
  - 已在瀏覽器裡實測過：單次點擊只加 1 級、模擬持續按住會連續加級且正確扣書、點滿當下自動停止不多扣書、書用完時不會硬扣出負庫存、關閉框框後角標與數值格會同步刷新。
- **技能點強化是永久的**：跟角色等級／解鎖一樣不受團滅影響（`endRun()` 沒有重置 `lineLevels`），但**尚未使用的書會跟其他背包物品一起適用團滅歸零規則**（見上方「怪物掉落物」章節），撤退成功才安全。
- **素材**：能力書與技能書分別使用 `assets/item/exp_book_stat.png` 與 `assets/item/exp_book_skill.png`，已接入背包、掉落與強化 UI。`exp_book_1.png` 是分級方案時的舊素材，目前程式不使用。
- **點滿一條線要 100 本書、7 條線要 700 本**：原本重複點擊感的問題（當時決定「批量使用先不做」）後來直接用上面的「按住持續升級」解決掉了，不用再另外做批量按鈕。

## 角色解鎖系統

- 遊戲開始只解鎖**無名**。
- **小初**：累計擊殺 **10 隻史萊姆**後解鎖。擊殺數是永久進度，不因撤退或團滅歸零。
- **豐子**：累計成功使用滿 **10 次藥水**後解鎖；治療藥水與迅捷藥水都會計數，已接入 `potionUseCount` 與通用解鎖檢查。
- **解鎖系統的通用架構與 UI 已實裝**：角色卡會顯示鎖定狀態與解鎖條件，鎖定角色無法選入隊伍；解鎖狀態與累計進度目前保留於本次執行期間，之後再由存檔系統負責跨裝置／重新開啟遊戲的永久保存。

## 角色專屬戰鬥操作

- 每名角色可擁有一個獨立的手動戰鬥操作，資料放在 `CHAR_DEFS.<角色>.action`，有自己的效果、圖示與冷卻；與三個自動技能、藥水、主動遺物分開。
- **無名「臨機應變」已實裝**：使用 `wuming_action.png`，手動點擊後隨機立即施放無名三個技能之一，獨立冷卻 18 秒；被抽中的技能會正常重設自己的技能冷卻。
- 準備角色卡會把專屬操作顯示為第 4 個小圖示，以金色外框與角落光點區分三個自動技能；準備階段只能 hover 查看，實際施放仍使用戰鬥操作列的 50×50 按鈕。
- 角色專屬操作使用通用控制狀態鎖：`STATUS_DEFS` 中標記 `blocksCharacterAction: true` 的狀態會自動禁用角色操作。目前睡眠與魅惑皆會鎖住操作；藥水屬於玩家外部操作，不受影響。未來暈眩、凍結等狀態可沿用同一標記。
- 專屬操作因控制狀態被鎖住時，按鈕會覆蓋半透明的 `assets/skill_lock.png`；一般冷卻仍使用原本的黑色冷卻遮罩與秒數，讓「被控制」和「尚未冷卻」有不同視覺語言。

## 主動發動道具（玩家手動觸發的「第四招」）

- 跟角色原本 3 個技能不同：那三個是角色 AI 自動判斷冷卻好就出招，這個是**玩家自己手動按**，冷卻好了自己決定要不要按下去。UI 上會沿用跟技能一樣的圖示＋冷卻條視覺語言，但要能點擊、冷卻中要看起來明顯不能按。
- 這其實延續了遊戲已經有的設計哲學：首領戰的黏液點擊小遊戲就已經是「AI 自動打，但玩家保留關鍵時刻可以介入」的模式，這個道具槽是同一種精神的延伸，不算破壞掛機的核心體驗。
- **定案：跟著角色走，每個角色各自一個道具槽**（不是整隊共用一個）。之後開放多人連線、同時帶 3 人時，會是 3 個角色各自獨立的道具槽。
- 效果定位：**刻意做成「可有可無的簡單輔助效果」**，不做強力技能，例如：小治療、解除減益、給我方上增益、給敵方上減益、加速冷卻等。
- **重複獲得可以合成**：拿到同一種道具的重複品可以合成升級成更強版本（合成規則、稀有度分層尚未定案）。
- 取得管道：**商店賣藥水以外的這類道具，事件也可能給**。
- **合成規則定案**：分五個稀有度——初階／中階／高階／頂級／完美，**每 3 個同稀有度合成 1 個高一階**。換算下來，若完全只靠初階道具往上疊，堆出 1 個完美需要 3⁴ = **81 個初階**。稀有度分層、道具池具體內容留到動工時再討論。

## 藥水

- **效果目前只有回血一種**，沒有其他附加效果。
- 取得管道：商店販售。
- **定案：不做自動觸發**，藥水維持純手動——玩家自己按，行為跟主動道具一樣。原本考慮的「AI 自動判斷 HP 該喝了」整套判斷邏輯（閾值、全域開關 vs 逐瓶開關）直接不做，省掉一整塊需要調參、容易誤判的系統。
- **手動使用已實裝**：戰鬥中點擊藥水按鈕，會消耗背包 1 瓶並治療生命比例最低的存活角色 30% 最大生命值，冷卻 10 秒。沒有藥水、冷卻中或全員滿血時按鈕不可使用；冷卻以遮罩與秒數顯示。
- 出戰前必須先裝備藥水：可以從背包點擊藥水直接放入，也可以使用角色卡上的選擇框。準備畫面中，戰鬥道具格會顯示在目前選中的角色卡上、位於遺物格旁邊，但這只是視覺歸位，資料仍屬於玩家操作列而不綁定角色；遠征開始後鎖定至本次遠征結束。
- 點擊角色卡上的戰鬥道具格會在旁邊開啟小型選擇框，列出可裝備道具與持有數量，也能選擇不攜帶；點框外或按 Esc 關閉。從背包直接點擊裝備的方式仍保留。
- 成功使用藥水會累計永久使用次數，滿 10 次後透過既有解鎖架構解鎖豐子；跨重新開啟遊戲的保存仍等待存檔系統。
- 新增黃色「迅捷藥水」（`item/speed_potion.png`）：使用後使全隊攻速提升 50%，持續 30 秒，使用冷卻 10 秒；測試背包暫放 2 瓶。它與治療藥水共用資料驅動的戰鬥道具、裝備與使用流程，使用後同樣計入豐子的藥水累計次數。
- 修正戰鬥畫面每 100ms 重建藥水按鈕內容、可能在滑鼠按下與放開之間取消 click 的問題；按鈕 DOM 現在保持穩定，只更新數量、冷卻遮罩與倒數文字。

## 背包 UI（基礎功能已實裝）

- 形式定案：**彈窗（overlay/modal）**蓋在目前畫面上，不做成獨立整頁——關掉彈窗就回到原本的準備／戰鬥畫面，不打斷遊戲進度。
- 開發順序定案：**先做外殼＋假資料**（背包 UI、物品格子、hover 說明都做出來，先塞 1～2 個測試用藥水），等商店／事件系統的真實掉落與購買邏輯確定後，再串接真正的資料。
- **外殼＋假資料已實裝**：標題列加入背包按鈕，準備與戰鬥期間都能開啟；彈窗包含 8 個固定槽位、數量與 hover 說明，目前放入測試藥水與 3 顆測試魔物結晶。物品可以拖曳到空格或互換位置，拖曳只整理背包、不會使用物品。開啟背包不會暫停戰鬥，點擊外側、關閉按鈕或 Esc 都能關閉。
- **快捷使用區已實裝**：藥水是獨立、不綁角色的戰鬥操作，可在出戰前從背包或角色卡道具格裝備，戰鬥中會正確消耗、觸發效果與冷卻。主動遺物格仍保留，但遺物效果與配裝尚未實作。
- 遊戲頁面已停用瀏覽器右鍵選單，避免拖曳背包物品或戰鬥操作時被原生選單打斷；只影響遊戲頁面本身。

## 戰鬥畫面配置

- 桌面版改為左右雙欄：左側放敵方卡片與 `bossArena`，右側放玩家卡片與手動戰鬥操作，戰鬥紀錄維持在兩欄下方。
- 畫面寬度不足 850px 時自動回到上下單欄，避免卡片被過度壓縮。
- 戰鬥角色卡不常駐顯示 EXP 條；經驗值保留在角色 hover 詳細資訊中，讓敵我卡片高度與資訊密度更接近。
- 怪物卡與怪物 tooltip 會顯示等級；目前怪物等級跟隨樓層，第一層一般怪物與史萊姆王皆為 Lv.1。
- 傷害計算的小數使用機率取整：例如最終值 1.8 有 80% 變成 2、20% 變成 1，長期平均仍維持 1.8；最低傷害仍為 1。
- 藥水與主動遺物的操作格統一使用純正方形圖示；名稱、效果與持有數量改由 hover tooltip 顯示。
- 戰鬥操作列不在遺物格上方標示角色名稱，藥水與遺物保持同高排列；所屬與效果資訊由 tooltip／後續角色詳情承擔。
- 史萊姆王卡片不再顯示額外的「首領戰」提示文案，名稱固定為紅色「史萊姆王」；王戰期間戰鬥紀錄維持一般高度，不再縮小。
- 角色狀態圖示改為浮在肖像右上角，不占角色卡排版高度；最多直接顯示 4 個，超出的狀態收為 `+N`。剩餘秒數不常駐，改到 hover tooltip 顯示。
- 史萊姆王第三招「黏液陣」改為每次同時生成 3 顆黏液：必須在時限內清完整批才一次結算傷害；完美點擊會提高最後總傷害，任一顆化開則整批失敗並增加一層沾黏。同時間只存在一批。

## 怪物掉落物

- 一般怪物有機率掉落「魔物結晶」（`item/stone.png`），機率制，不是必掉。簡短設定：怪物掉落的漂亮結晶，可以賣個好價錢。
- **第一版掉落已實裝**：一般、非召喚的史萊姆死亡時有 35% 機率掉落 1 顆魔物結晶；召喚物不掉落，避免透過史萊姆王無限刷素材。掉落會優先堆疊，每格上限 99，背包滿時會在戰鬥紀錄提示無法拾取。
- 本次遠征撿到的魔物結晶會另外追蹤：撤退或通關後正式保留，團滅則只扣除本次遠征取得的數量，不影響原本已安全持有的結晶。
- 怪物死亡退場改為先灰階停留片刻，再淡出縮小，約 1.1 秒後移除卡片，避免原本 0.5 秒直接消失顯得突兀。
- **地城內商店可以直接把魔物結晶賣掉換錢**——賣掉的錢算進 `runGold`（跟撿到的金幣同一個桶子），也就是說結晶本身跟賣出去的錢，在撤退成功之前都算「未入袋」，團滅一樣會蒸發。
- 城外「倉庫」其實就是既有的 `bankedGold` 概念換了個敘事包裝（安全、永久），不是新系統，兩者是同一套規則。
- 之後魔物結晶會分稀有度／不同種類（目前只有一種），細節等動工時再聊。

## 地城商店

- 第一版已實裝於清完小怪、進入史萊姆王前：魔物結晶每顆可賣 5 金幣，治療藥水售價 12 金幣，迅捷藥水售價 18 金幣。
- 商店可一鍵賣出全部魔物結晶；購買的藥水直接進背包並視為本次遠征所得，撤退／通關後保留，團滅時失去尚未使用的部分。背包無空間時不扣錢。
- 10 秒沒有成功買賣會自動離開；每次成功交易會重設 10 秒倒數。離開後回到首領戰前確認畫面，商店不會在同一次遠征重複開啟。
- 地城商店的 10 秒自動離開可以在商店內關閉或重新開啟；城外商店沒有倒數。
- 城外準備階段可從標題列開啟商店，使用安全的 `bankedGold` 買賣；地城商店使用本局 `runGold`。魔物結晶可選擇賣 1 顆或一鍵全部賣出。

## 事件系統

- 形式定案：偏 **Slay the Spire 式**——遇到岔路，隨機小事件，給選項，有好有壞。
- **不需要卡片**：Slay the Spire 事件本來就不是只換卡片，也常見金幣、遺物、藥水、拿 HP 換屬性這類選項。這款遊戲可以直接用**現有已經存在的資源**當獎勵池，不需要等遺物或藥水系統蓋好才能動工：
  - 賭博型：獻祭一定比例目前生命值，換取本局暫時性攻擊力加成
  - 純數值：撿到一袋金幣／踩到陷阱扣血
  - 效率型：打坐休息回滿血，但跳過這層剩餘的小怪
  - 之後藥水／掉落系統做好了，可以再加「獲得隨機藥水」之類的選項進去，不影響現在先上線的版本
- **事件跟商店都適用同一條「10 秒無操作自動離開」規則**，是為了配合掛機玩法設計的——不能讓一個彈窗卡住掛機進度逼玩家守在螢幕前。逾時的預設行為：**視同「不選/跳過」**，不觸發任何效果（商店逾時＝空手離開，事件逾時＝不拿獎勵也不吃懲罰）。

## 下一步

- **當前優先順序**：先完成無名與共用系統；小初、豐子保留解鎖與既有技能資料，暫不擴寫專屬內容。
- **史萊姆王的黏液點擊小遊戲決定維持在獨立的 `bossArena`**，不移到玩家角色卡或肖像上。
- **目前只開放樓層 1**：擊敗史萊姆王後視為遠征成功，本局金幣自動入袋並回到外部準備畫面，不進入尚未製作的樓層 2。程式保留 `MAX_IMPLEMENTED_FLOOR` 上限，未來完成樓層 2 後再提高即可接回樓層遞進。

## 尚未討論 / 待補完

- 事件觸發頻率／時機：討論中，見下方。
- 具體事件內容清單：只列了範例方向，還沒真的列表（決定「做的時候再討論」）。
- 主動道具的稀有度分層細節、道具池具體內容：決定「做的時候再討論」。
- 主動道具跟角色綁定後，UI 要顯示在角色卡的哪裡（第 4 個技能格，還是獨立區塊）：決定「做的時候再討論」。
- 存檔檔名/路徑、兩台電腦衝突處理：決定「先放置不管」，之後真的要做再說。長期則想把存檔改成真的串伺服器、由伺服器記錄玩家資料（見上方「存檔系統」章節），也是先放著。
- **UI auto resize**：畫面（`#app` 固定寬度、卡片排版）之後考慮做自動縮放/自適應，目前只是先記錄這個念頭，細節（要適應什麼情境、縮放到多小）還沒討論。

## 素材命名規範（2026-09-01 整理過一次）

- **史萊姆變種：用個性/表情命名，不用編號**——`slime_angry.png`（紅，生氣，攻擊型配色）、`slime_sleepy.png`（紫，想睡，配色像毒/睡眠）、`slime_love.png`（粉，愛心眼）。理由：之後如果真的要給狀態效果（例如紫色附加睡眠），檔名本身就跟效果對上，不用另外查表。原本的 `slime2/3/4.png` 編號已改名，對應的技能圖 `slimeX_skill.png` 也一併改成 `slime_angry_skill.png` 等等。改名當下程式碼裡完全沒有引用到這些檔案，是零風險的重新命名。
- **角色素材使用角色 id 命名**：肖像為 `<角色id>.png`，完整立繪為 `<角色id>_full.png`，技能為 `<角色id>_skill1~3.png`，專屬手動操作為 `<角色id>_action.png`。未上線角色的素材可留在 `assets/`，不需先接入 `CHAR_DEFS`。
- 準備畫面短按角色卡維持選擇出戰；長按約 450ms 才開啟角色詳情視窗。詳情使用 `<角色id>_full.png` 完整立繪、顯示生命／攻擊／防禦／行動間隔／經驗，並以 64×64 顯示技能與專屬操作。缺少 `_full` 或技能素材時顯示具名空框，不放大其他素材代替。滑鼠／手指移動超過容許距離會取消長按，避免拖動誤觸。
- 長按期間會在按住的位置顯示 450ms 金色環形進度；放開或移動取消時立即消失，繞滿後開啟角色詳情。
- **修正錯字**：`item/posion.png` → `item/potion.png`（原本打錯，這張其實是治療藥水，不是毒藥）。
- `item/` 資料夾目前有 `coin.png`（金幣圖示）、`potion.png`（藥水）、`stone.png`（怪物掉落的可販售素材，見下方「怪物掉落物」章節）。
- 未來新素材命名原則：如果檔名之後會拿來對應遊戲效果/用途，用描述性命名；如果只是內部索引、程式碼裡已經有對照表兜著，維持簡單編號即可，不用太糾結。

## 技術棧提醒（持續有效，不是聊完就結案）

開發量目前持續增加（商店、背包、技能點轉換、角色解鎖、之後還有遺物／事件），但截至目前仍判斷維持純 HTML/CSS/JS 沒問題。真正該重新評估的訊號是：`render()` 需要同時處理多個會互相影響資料、且互斥的整頁畫面時，現在「全域變數＋手動操作 DOM」的寫法會開始難維護——屆時建議的解法是引入輕量的畫面狀態管理，而不是整個換成應用程式框架或 Electron／Tauri（那是「怎麼發布」的問題，跟現在的架構選擇是兩件事）。背包目前決定做成彈窗，複雜度還在可控範圍內，尚未觸發這個訊號。

- **2026-09-01 健檢**：重新看過 `constants.js`／`state.js`／`combat.js`／`ui.js` 之後，架構還沒踩到上面的訊號，維持純 HTML/CSS/JS 沒問題。`activeOverlay`（`state.js`）＋ `OVERLAY_CLOSERS` 註冊表（`ui.js`）已經是輕量畫面狀態管理的雛形——單一事實來源決定目前開哪個彈窗，開新的會自動收掉舊的，提前把「多個互斥整頁畫面」這個原本擔心的問題處理掉了。
  - **`tick()` 拆函式已完成**：`combat.js` 的 `tick()` 原本 ~140 行混了角色行動／buff 倒數／王的特殊機制／怪物行動／死亡掃描五段邏輯，已拆成 `tickCharacters()`／`tickBuffs()`／`tickMonsters()` 三個子函式，`tick()` 本身只剩「團滅檢查 → 依序呼叫三者 → 死亡掃描 → render()」，純內部重構、行為不變。
